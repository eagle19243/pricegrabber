const appStyle = theme => ({
  backdrop: {
    zIndex: theme.zIndex.drawer + 1,
    color: "#fff",
  },
  alert: {
    paddingLeft: "30px",
    paddingRight: "30px",
    paddingTop: "15px",
    paddingBottom: "15px",
  }
});

export default appStyle;
